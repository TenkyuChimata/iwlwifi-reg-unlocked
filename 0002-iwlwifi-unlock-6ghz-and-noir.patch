diff -urN '--exclude=*.o' '--exclude=*.ko' '--exclude=*.mod' '--exclude=*.mod.c' '--exclude=*.mod.o' '--exclude=*.cmd' '--exclude=*.order' '--exclude=*.symvers' '--exclude=Module.symvers' '--exclude=modules.order' '--exclude=.tmp_versions' orig/drivers/net/wireless/intel/iwlwifi/iwl-nvm-parse.c mod/drivers/net/wireless/intel/iwlwifi/iwl-nvm-parse.c
--- orig/drivers/net/wireless/intel/iwlwifi/iwl-nvm-parse.c	2026-03-24 15:40:52.335874063 +0800
+++ mod/drivers/net/wireless/intel/iwlwifi/iwl-nvm-parse.c	2026-03-24 15:53:22.248791927 +0800
@@ -274,7 +274,7 @@
 static u32 iwl_get_channel_flags(u8 ch_num, int ch_idx, enum nl80211_band band,
 				 u32 nvm_flags, const struct iwl_rf_cfg *cfg)
 {
-	u32 flags = IEEE80211_CHAN_NO_HT40;
+	u32 flags = 0;
 
 	if (band == NL80211_BAND_2GHZ && (nvm_flags & NVM_CHANNEL_40MHZ)) {
 		if (ch_num <= LAST_2GHZ_HT_PLUS)
@@ -287,7 +287,7 @@
 		else
 			flags &= ~IEEE80211_CHAN_NO_HT40MINUS;
 	}
-	if (!(nvm_flags & NVM_CHANNEL_80MHZ))
+	/* if (!(nvm_flags & NVM_CHANNEL_80MHZ))
 		flags |= IEEE80211_CHAN_NO_80MHZ;
 	if (!(nvm_flags & NVM_CHANNEL_160MHZ))
 		flags |= IEEE80211_CHAN_NO_160MHZ;
@@ -302,7 +302,7 @@
 		flags |= IEEE80211_CHAN_RADAR;
 
 	if (nvm_flags & NVM_CHANNEL_INDOOR_ONLY)
-		flags |= IEEE80211_CHAN_INDOOR_ONLY;
+		flags |= IEEE80211_CHAN_INDOOR_ONLY; */
 
 	/* Set the GO concurrent flag only in case that NO_IR is set.
 	 * Otherwise it is meaningless
@@ -312,12 +312,14 @@
 		flags |= IEEE80211_CHAN_IR_CONCURRENT;
 
 	/* Set the AP type for the UHB case. */
-	if (nvm_flags & NVM_CHANNEL_VLP)
+	/* if (nvm_flags & NVM_CHANNEL_VLP)
 		flags |= IEEE80211_CHAN_ALLOW_6GHZ_VLP_AP;
 	else
 		flags |= IEEE80211_CHAN_NO_6GHZ_VLP_CLIENT;
 	if (!(nvm_flags & NVM_CHANNEL_AFC))
-		flags |= IEEE80211_CHAN_NO_6GHZ_AFC_CLIENT;
+		flags |= IEEE80211_CHAN_NO_6GHZ_AFC_CLIENT; */
+
+	flags |= IEEE80211_CHAN_ALLOW_6GHZ_VLP_AP;
 
 	return flags;
 }
@@ -394,7 +396,7 @@
 			 */
 			iwl_nvm_print_channel_flags(dev, IWL_DL_EEPROM,
 						    nvm_chan[ch_idx], ch_flags);
-			continue;
+			/* continue; */
 		}
 
 		channel = &data->channels[n_channels];
@@ -1144,8 +1146,8 @@
 	    !iwlwifi_mod_params.disable_11ax)
 		iwl_init_he_hw_capab(trans, data, sband, tx_chains, rx_chains,
 				     fw);
-	else
-		sband->n_channels = 0;
+	/* else
+		sband->n_channels = 0; */
 
 	if (n_channels != n_used)
 		IWL_ERR_DEV(dev, "NVM: used only %d of %d channels\n",
@@ -1532,7 +1534,7 @@
 				int ch_idx, u16 nvm_flags,
 				struct iwl_reg_capa reg_capa)
 {
-	u32 flags = NL80211_RRF_NO_HT40;
+	u32 flags = 0;
 
 	if (ch_idx < NUM_2GHZ_CHANNELS &&
 	    (nvm_flags & NVM_CHANNEL_40MHZ)) {
@@ -1551,7 +1553,7 @@
 		flags &= ~NL80211_RRF_NO_HT40MINUS;
 	}
 
-	if (!(nvm_flags & NVM_CHANNEL_80MHZ))
+	/* if (!(nvm_flags & NVM_CHANNEL_80MHZ))
 		flags |= NL80211_RRF_NO_80MHZ;
 	if (!(nvm_flags & NVM_CHANNEL_160MHZ))
 		flags |= NL80211_RRF_NO_160MHZ;
@@ -1563,7 +1565,7 @@
 		flags |= NL80211_RRF_DFS;
 
 	if (nvm_flags & NVM_CHANNEL_INDOOR_ONLY)
-		flags |= NL80211_RRF_NO_OUTDOOR;
+		flags |= NL80211_RRF_NO_OUTDOOR; */
 
 	if (nvm_flags & NVM_CHANNEL_ALLOW_20MHZ_ACTIVITY &&
 	    flags & NL80211_RRF_NO_IR)
@@ -1585,7 +1587,7 @@
 	}
 
 	/* Set the AP type for the UHB case. */
-	if (nvm_flags & NVM_CHANNEL_VLP) {
+	/* if (nvm_flags & NVM_CHANNEL_VLP) {
 		if (!(nvm_flags & NVM_CHANNEL_VLP_AP_NOT_ALLOWED))
 			flags |= NL80211_RRF_ALLOW_6GHZ_VLP_AP;
 	} else {
@@ -1593,12 +1595,14 @@
 	}
 
 	if (!(nvm_flags & NVM_CHANNEL_AFC))
-		flags |= NL80211_RRF_NO_6GHZ_AFC_CLIENT;
+		flags |= NL80211_RRF_NO_6GHZ_AFC_CLIENT; */
+
+	flags |= NL80211_RRF_ALLOW_6GHZ_VLP_AP;
 
 	/*
 	 * reg_capa is per regulatory domain so apply it for every channel
 	 */
-	if (ch_idx >= NUM_2GHZ_CHANNELS) {
+	/* if (ch_idx >= NUM_2GHZ_CHANNELS) {
 		if (!reg_capa.allow_40mhz)
 			flags |= NL80211_RRF_NO_HT40;
 
@@ -1616,7 +1620,7 @@
 		flags |= NL80211_RRF_NO_HE;
 
 	if (reg_capa.disable_11be)
-		flags |= NL80211_RRF_NO_EHT;
+		flags |= NL80211_RRF_NO_EHT; */
 
 	return flags;
 }
